global:
  scrape_interval: 2s
  evaluation_interval: 2s

rule_files:
  - /etc/prometheus/rules/harmost-alerts.yml

scrape_configs:
  - job_name: harmost
    static_configs:
      - targets: ["harmost:9090"]
